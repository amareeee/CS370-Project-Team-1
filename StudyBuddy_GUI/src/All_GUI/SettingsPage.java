package All_GUI;

import javax.swing.*;
import java.awt.*;

public class SettingsPage extends JPanel {
    public SettingsPage() {
        setLayout(new BorderLayout());
        setBackground(Color.PINK);
        add(new JLabel("Settings Page"), BorderLayout.CENTER);
    }
}
